# `status` is published by the schema as `examples`, not as an `enum`, so the
# schema cannot object to a value outside the conventional set — and does not.
# This document is conformant. The hygiene band says something about it anyway,
# which is a rule that could not exist if errors and warnings were one channel.
version: 1.0.0
apiVersion: v3.1.0
kind: DataContract
id: 8c5eb4d9-0b4d-4c8a-9c2e-8c1a0a4a6b77
status: mothballed
description:
  purpose: Kept for audit only.
servers:
  - server: archive
    type: s3
    location: s3://example-archive/orders/*.parquet
schema:
  - name: orders
    logicalType: object
team:
  name: Archive Keepers
